apiVersion: v1
kind: Service
metadata:
  name: k8s-web-service
  namespace: k8s-web
spec:
  type: NodePort
  selector:
    app: k8s-web
  ports:
    - port: 80
      protocol: TCP
      targetPort: 5000
      nodePort: 30081
