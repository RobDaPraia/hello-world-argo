# Flask Configuration
PORT=5000
FLASK_ENV=development

# Azure Configuration (for local development with Azure CLI)
# These values are used when running locally with 'az login' credentials
# In production, these are automatically injected by the GAH platform
# APP_STORAGE_ACCOUNT_NAME=<secret>
# APP_KEY_VAULT_NAME=<secret>
